/**
 * Shared durations for transient success/confirmation UI - how long a toast,
 * flash state, or brief success modal stays up before auto-resetting.
 */

/** How long a transient success indicator (label swap, flash state, dismissible banner) stays up before auto-resetting. */
export const TOAST_AUTO_DISMISS_MS = 2500;

/** How long a brief "success" confirmation modal stays open before auto-closing. */
export const SUCCESS_MODAL_CLOSE_MS = 900;

/** How long the final submit-success overlay stays up before redirecting away. */
export const SUBMIT_SUCCESS_REDIRECT_MS = 1100;
